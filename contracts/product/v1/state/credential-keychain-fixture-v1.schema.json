{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://krx-cli.invalid/contracts/state/credential-keychain-fixture-v1.schema.json",
  "title": "Test-only keychain fixture",
  "description": "Production keychain state is never represented as JSON.",
  "type": "object",
  "required": ["version", "service", "account", "secret"],
  "properties": {
    "version": { "const": 1 },
    "service": { "const": "krx-cli" },
    "account": { "const": "default" },
    "secret": { "type": "string", "minLength": 1 }
  },
  "additionalProperties": false
}
